# DynamicPackage 行为等价重建：GitHub 轻量镜像说明

**镜像基线：** 本文件对应本地恢复工程提交 `f80c98a7b08b6b643104965f5cd8e0be48a7423c`（`审计：梳理恢复C11数值与ABI理论边界`）。该版本包含 H0–H36 的恢复源码、可复现比较器、原 ELF 调试探针、Makefile 门禁、覆盖审计和中文证据文档。

> 本仓库是**轻量协作镜像**，不是完整二进制证据库。它用于审阅恢复 C11 源码、理解测试方法、检查提交历史并在拥有受限输入与完整本地证据时复现指定合同。

## 已包含的内容

| 内容 | 用途 |
|---|---|
| `src/` | 当前恢复 C11 源码、ABI 头文件和静态参数快照。 |
| `Makefile` | 严格 C11 构建与各项独立/默认门禁定义。 |
| `analysis/**/*.c`、`analysis/**/*.gdb` | 恢复端比较器与仅在调试副本上运行的原 ELF 采集探针。 |
| `analysis/**/*.md`、小型摘要日志 | 覆盖边界、gold 生成步骤、差分结论、H28 修复证据和 H36 理论审计。 |

## 特意未包含的内容

| 未上传内容 | 原因 |
|---|---|
| `input/DynamicPackage.elf`、调试执行副本及任何 ELF | 原始样本保持本地只读；不得通过该镜像分发。 |
| 大体积逐步 gold `.bin` | 100,000 步 IPC 单文件可达数百 MiB；这些是本地可再生成的证据而非协作镜像的必需内容。 |
| 完整 `selftest` 重复日志与构建产物 | 单份日志约 50–60 MiB，重复信息密度低；镜像仅保留小型摘要及复现命令。 |
| 共享内存对象、临时目录或环境文件 | 避免泄露运行时状态并确保仓库可移植审阅。 |

完整受控证据的生成方法和归档策略见 [`analysis/coverage_inventory/GOLD_GENERATION_AND_GITHUB_ARCHIVING.md`](analysis/coverage_inventory/GOLD_GENERATION_AND_GITHUB_ARCHIVING.md)。若在此镜像中运行依赖未上传 gold 的完整 `make selftest`，相应目标会因缺少本地 gold 文件而不能复现；这不是恢复 C 与原 ELF 的差异。

## 当前验证范围与限制

当前本地证据覆盖 28 条结构化 `dyn_main` 主路径，包含圆赤道与 `e=0.99` 高偏心根数边界、多个初始化/命令/IPC 序列。对列明的当前 Linux x86-64、运行时、编译选项和固定受控输入，比较器逐步检查内部状态和可观察输出的原始字节。

该结果不等于原始源码恢复，不证明任意输入、任意平台、并发/异常资源条件或真实物理系统的正确性。工程只能用于**离线研究、教学、受控仿真和回归**，不得用于真实飞行、姿态控制、实体执行机构、任务决策或其他安全关键用途。
